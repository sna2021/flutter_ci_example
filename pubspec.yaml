name: app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+18

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: '2.8.1'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  app_logger:
    git:
      url: git@github.com:smmarty/friflex_mobile_uikit.git
      path: app_logger

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
